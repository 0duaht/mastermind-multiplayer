require 'spec_helper'

describe Tobi::Mastermind do
  it 'has a version number' do
    expect(Tobi::Mastermind::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
